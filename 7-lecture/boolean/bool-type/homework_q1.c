#include <stdbool.h>
#include <stdio.h>

int main(){

    bool isStudent = true;
    bool isGraduate = false;
    
    printf("isStudent is:%d\n", isStudent);
    printf("isGraduate is:%d", isGraduate);

    return 0;

}