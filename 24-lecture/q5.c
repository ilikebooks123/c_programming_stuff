#include <stdio.h>

int main(){
    int n1;
    int n2;
    float source[n1][n2];
    float target[n1][n2];
    scanf("number of rows:%d", &n1);
    scanf("number of cols:%d", &n2);
    printf("%d %d", n1, n2);
}