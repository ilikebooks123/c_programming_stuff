#include <stdio.h>

int main(){
    char letter = 'A';
    putchar(letter); //Outputs the charater 'A' to the screen
    putchar('\n'); //outputs a newling character for better formatting

    return 0;

}