#include <stdio.h>
int compary_array(int arrA[],int arrB[],int arrA_len,int arrB_len){
    
}
void main(void){
    int arrayA[5] = {3,6,1,2,-4};
    int arrayB[6] = {7,9,-1,2,6,8};
    int arrayA_len = ;//complete the calculation
    int arrayB_len = ;//complete the calculation
    
    int result = compary_array(arrayA, arrayA_len, arrayB, arrayB_len);
    
    printf("The result is %d", result);
}
